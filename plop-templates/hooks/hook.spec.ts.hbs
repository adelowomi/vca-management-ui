import { renderHook } from '@testing-library/react-hooks';
import { render } from '@testing-library/react';
import {{camelCase name}} from './{{camelCase name}}';

describe('{{camelCase name}}', () => {
  it('', () => {
    renderHook(() => use{{camelCase name}}());
  });
});
